# Support Google Chrome in the first public release

The first public BrainHub Capture release officially supports Google Chrome and is distributed through the Chrome Web Store. Other Chromium browsers are outside the initial compatibility promise even if they can install the extension, because their identity APIs, store installation path, and Google OAuth behavior have not been independently verified.
