# Update only through the Chrome Web Store

Official BrainHub Capture releases update only through the Chrome Web Store. The extension does not install a custom updater, distribute an off-store CRX as the primary channel, or load remotely hosted executable code. Store updates preserve extension-local configuration and independently version the extension from BrainHub MCP.
