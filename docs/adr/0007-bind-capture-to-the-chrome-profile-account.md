# Bind BrainHub Capture to the Chrome Profile account

BrainHub Capture uses the primary Google account of the current Chrome Profile instead of implementing a separate account chooser. The extension must display the connected email so users do not confuse this account with an account selected inside a Google website. Users who need another Drive use the corresponding Chrome Profile.
