# Distribute the browser extension and MCP independently

The BrainHub browser extension and local MCP are separate installation products. Each can be installed, upgraded, authorized, and removed without installing the other; there is no combined installer. They interoperate through the user's personal Drive when both are present, while web-only and CLI-only users remain supported.
